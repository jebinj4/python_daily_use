NumPy
Matplotlib
Scikit-Image
Pandas

# To install the required packages, run the following command in your terminal:
# pip install -r requirements.txt


# if using mac use the below code
# /opt/homebrew/bin/python3 -m pip install numpy matplotlib scikit-image pandas --break-system-packages